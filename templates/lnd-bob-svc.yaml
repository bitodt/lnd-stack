---
apiVersion: v1
kind: Service
metadata:
  name: {{template "bob.fullname" .}}-svc
spec:
  selector:
    app: {{template "bob.fullname" .}}
  ports:
    - protocol: TCP
      port: 9735
      targetPort: 9735
